# frozen_string_literal: true

source "https://rubygems.org"

# The site generator itself (generate.rb) uses only the Ruby standard library.
# These gems are just for previewing the generated ./public site locally:
#   bundle install
#   bundle exec puma -p 8000        # http://localhost:8000
gem "puma", "~> 6.4"
gem "rack", "~> 3.1"
